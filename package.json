{
  "name": "kitn",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "registry"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck",
    "build:registry": "bun run --cwd registry build"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/bun": "^1.3.9"
  }
}
