{
  "name": "mongodb-storage",
  "type": "kitn:storage",
  "description": "MongoDB storage adapter — document-based storage using the native MongoDB driver",
  "dependencies": [],
  "registryDependencies": ["core"],
  "files": ["mongodb-storage.ts"],
  "docs": "Install `mongodb`. Call `createMongoStorage({ client: mongoClient, database: 'my-db' })`. Collections are auto-created. Indexes are created for common query patterns.",
  "categories": ["storage", "mongodb", "database", "nosql"],
  "changelog": [
    { "version": "1.0.0", "date": "2026-03-01", "type": "initial", "note": "Initial release" }
  ]
}
