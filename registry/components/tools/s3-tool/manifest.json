{
  "name": "s3-tool",
  "type": "kitn:tool",
  "description": "S3-compatible object storage — list, get, put, delete, and generate presigned URLs (AWS S3, Cloudflare R2, MinIO, Backblaze B2)",
  "dependencies": ["ai", "zod"],
  "registryDependencies": ["core"],
  "envVars": {
    "S3_ACCESS_KEY_ID": { "description": "S3-compatible access key ID", "required": true, "secret": true },
    "S3_SECRET_ACCESS_KEY": { "description": "S3-compatible secret access key", "required": true, "secret": true },
    "S3_ENDPOINT": { "description": "S3-compatible endpoint URL (e.g. https://s3.amazonaws.com, https://<account>.r2.cloudflarestorage.com)", "required": true, "secret": false },
    "S3_REGION": { "description": "S3 region (default: auto)", "required": false, "secret": false }
  },
  "files": ["s3-tool.ts"],
  "docs": "S3-compatible object storage tool. Works with AWS S3, Cloudflare R2, MinIO, Backblaze B2, and any S3-compatible provider. Set S3_ACCESS_KEY_ID, S3_SECRET_ACCESS_KEY, S3_ENDPOINT env vars. Supports presigned URL generation for direct uploads/downloads.",
  "categories": ["api", "storage", "s3", "cloud"],
  "changelog": [
    { "version": "1.0.0", "date": "2026-03-01", "type": "initial", "note": "Initial release" }
  ]
}
